[project]
name = "claude01"
version = "0.1.0"
description = "Cisco Device Scanner - Scans routers and switches for interface information"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "netmiko>=4.3.0",
]
